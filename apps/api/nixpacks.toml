# Fallback if Dockerfile is not used (Nixpacks)
# Prefer Dockerfile + railway.toml for DigiSEO on Railway.

[phases.setup]
nixPkgs = ["python312", "gcc"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
